import 'package:flutter/material.dart';

class MachinesListPage extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("List machines"),
    );
  }
}